{
  "name": "autoresearch-interface",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Six-axis robot arm CAD animation in the Nautilus model-library style: assembled and exploded views, both articulated.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc -p tsconfig.json && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "model": "node scripts/build-robot-arm.mjs",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "three": "^0.171.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.6.2",
    "@types/three": "^0.171.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.6"
  }
}
